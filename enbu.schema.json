{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "enbu.toml",
  "description": "enbu keyless .env management configuration",
  "type": "object",
  "required": ["version"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "enum": ["v1alpha1"],
      "description": "Config schema version. Must be \"v1alpha1\"."
    },
    "default_env": {
      "type": "string",
      "description": "Default environment name. Defaults to \"default\" if omitted."
    },
    "env": {
      "type": "object",
      "description": "Environment definitions. Each key is the environment name. Omit entirely to use auto-generated output paths.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "output": {
            "type": "string",
            "description": "Output file path for this environment. Defaults to .env.{name} (or .env for the \"default\" environment)."
          }
        }
      }
    }
  }
}
